fastapi
uvicorn[standard]
sqlalchemy
alembic
psycopg2-binary
python-jose[cryptography]
passlib[bcrypt]
bcrypt<4.1          # passlib 1.7.4 cannot drive bcrypt >= 4.1 (reads bcrypt.__about__)
pydantic[email]
pydantic-settings
python-multipart
slowapi
# S3-compatible object storage for event images. Only used when the S3_*
# settings are present; development and the tests write to local disk.
boto3

# dev / tests
pytest
httpx
